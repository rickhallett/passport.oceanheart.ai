# Environment
GO_ENV=development
PORT=3000

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/passport_buffalo_development?sslmode=disable

# Security
SECRET_KEY_BASE=your-secret-key-base-here-min-64-chars
SESSION_SECRET=your-session-secret-here-min-64-chars

# SMTP (for password reset emails)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=your-sendgrid-api-key
SMTP_FROM=noreply@oceanheart.ai

# SSL (production only)
SSL_HOST=passport.oceanheart.ai